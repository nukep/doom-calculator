(ns doomcalc.core
  (:require [doomcalc.digits :as digits]
            [doomcalc.write-pwad :refer [spit-pwad]]
            [doomcalc.wad-builder :as w]
            [doomcalc.tree :as t]
            [doomcalc.logic :as logic]
            [doomcalc.levelparts]))

(defn -main [])
